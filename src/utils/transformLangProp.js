export default function transformLangProp(movies, languajes) {
  // const newMovies = movies.map(movie => (
  //     // if(languages[movie.])
  // ))
}

// son dos array de objetos.
// tengo que chekear por cada movie, si su propiedad "original_language" se encuentra como valor,
// de la propiedad iso_639_1 de  alguno de los lenguajes [{english_name: "Latin" iso_639_1: "la" name: "Latin"}]

// si se encuentra, hay que remplazar la prop  "original_language" de la movie, por el valor de "name" en el lenguaje.
